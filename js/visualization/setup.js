const scenes = {
  current: 0,
  views: [new THREE.Scene(),new THREE.Scene(),new THREE.Scene()],
  switchScene: (i)=>{

  }
}
const controllers = {
  display: document.getElementById('WebGL-Output')
}

function initScene() {
  
}